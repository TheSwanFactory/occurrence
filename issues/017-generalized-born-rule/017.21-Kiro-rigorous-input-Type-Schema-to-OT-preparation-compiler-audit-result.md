# 017.21 — Kiro result: rigorous input Type Schema to OT preparation compiler audit

- **Executed:** 2026-09-22
- **Executor:** Kiro
- **In-Reply-To:** `017.20-Task-rigorous-input-Type-Schema-to-OT-preparation-compiler-audit.md`
- **Reviews honored:** `017.20a-GPT-owner-rationale-input-constitution-before-Interact-unification.md`, `017.20b-Canary-review-input-constitution-canonicality-fence.md`
- **Implementation base:** `occurrence@853f0371f675d911a2988f63c3eaeb5d2901bf6e` (`decision-model` 1.1.0)
- **Immutable implementation commit:** `occurrence@52a881f669a2729b6f8ff1067b505dd326309598`
- **Kind:** bounded exact research-coding execution; no training; no Interact
- **Disposition:** **STRONG NEGATIVE — C∅ UNDER THE EVIDENCE FENCE**

## One-paragraph answer

The banked abstract Fano point-line fixture passes the source gate, but current authority does **not** supply a source-authorized compiler from that incidence semantics to a distinguished Theory-27 preparation. It also does not supply a formal OT test realizing the declared `IncidenceDecision`. An exact conditional witness can be made after importing the rank-(2,14) projector test from 017.18, binding its slots, and choosing two preparation rays: it is valid, exact, zero-training, invariant on the two 21/28 source relation classes, non-collapsing, and uses one fixed test. That does not earn a positive compiler. Those target choices are constitutive; two projectively distinct witnesses are related by an explicit fixed-test-preserving symmetry; and no authority identifies that symmetry with the full OT representation gauge. The strongest task-level verdict is therefore **C∅**, localized at `source semantics ⇢ distinguished linear/projective preparation data`, with a separate missing `IncidenceDecision ⇢ formal test` bridge. No Training handoff is justified.

## A. Source

The exact domain object is one ordered pair

```text
(point, line) ∈ Point × Line
```

in the abstract finite projective plane `PG(2,2)`. The fixture contains:

```text
7 opaque Point values
7 opaque Line values
21 declared incidences
49 admitted point-line pairs
21 incident inputs
28 nonincident inputs
```

It is nonlinguistic, finite, exact, and has two independently meaningful components. Every point lies on three lines, every line contains three points, each point pair lies on one line, and each line pair meets at one point.

The abstract incidence reduct is banked by GPT Issue 011 and implemented independently of OT vocabulary in `topographo/core/f2_groups.py`. Its genealogy is nevertheless FIPS-derived, and that is not hidden here. The exact decision-bearing JSON used in this turn was written and byte-hashed before the conditional compiler code, but it has no separate prior immutable preregistration. Consequently the local ordering is not used as evidence for a positive result.

Frozen source:

```text
schema  gpt-01720-fano-incidence-source/v1
path    issues/017-generalized-born-rule/017.20-Code-attachments/source_fixture.json
sha256  20176b9043516e704fbffec84d01f515f008630aef9398d0f44d3cf96886d71c
```

The semantic payload contains no Event, probability, Born value, effect, preparation, ray coordinate, answer field, or per-instance target-state table.

## B. Type Schema

Beyond serialization, `Σ` declares:

1. distinct `Point` and `Line` sorts;
2. the 21-row point-line incidence relation;
3. all 49 admitted inputs;
4. raw-presentation equivalence through sort-preserving incidence automorphisms;
5. the fixed binary type `IncidenceDecision = incident | nonincident`; and
6. the global rule that the result is `incident` exactly on the declared relation.

Exhausting all `7! = 5,040` point relabelings recovers exactly 168 incidence automorphisms. Their action on `Point × Line` has exactly two orbits, of sizes 21 and 28, matching incident and nonincident.

The global schema rule completely determines the binary decision. This is not a hidden per-instance answer field, but it means the conditional evaluator is a symbolic conformance example, **not predictive evidence** and not a learning result.

## C. Compiler

No task-admitted compiler

```text
C_Σ : Input_Σ → Prep_OT
```

is supplied by the evidence fence.

The strongest executable construction is only conditional:

```text
C_{r_I,r_N}(point, line)
    = r_I  if incident(point, line)
    = r_N  otherwise.
```

The Type Schema supplies the relation-class factorization. The 017.18 evaluator witness supplies, constitutively rather than derivationally, one complementary diagonal projector test on `R^16`. The executor then supplies one ray `r_I` in the first effect face, one ray `r_N` in the second effect face, and a binding from semantic labels to test slots.

Two exact rational representatives were audited:

```text
A: incident ↦ [e0], nonincident ↦ [e2]
B: incident ↦ [e1], nonincident ↦ [e3]
```

Both use zero learned parameters and no per-instance lookup table. Their existence shows that a conditional plumbing witness is easy. It does not show that the source semantics or certified OT selected one.

Authority localization:

- Theory 27 takes `[x] ∈ P(V)` as supplied and explicitly leaves domain/physical preparation `→ [x]` open.
- Theory 41 forbids promoting Fano/FIPS/SCE source geometry into a formal target type without an explicit bridge.
- Theory 43 Encoding I requires noncanonical `U` and `φ`; Encoding II either starts from the forbidden Event vector or lands in `CP^7`, not the Theory-27 `P(R^16)` carrier.
- GPT 017.18 supplies an exact evaluator witness whose source is already `p`; it is not an input-constitution theorem and does not interpret `IncidenceDecision`.

## D. Invariance

For each conditional representative, all `168 × 49 = 8,232` automorphism/input pairs were checked exactly. Inputs in the same declared relation class compile to the same projective ray, so the conditional construction preserves the declared raw-presentation equivalence.

Full equivariance is not claimed. Current authority supplies no action of the 168 source automorphisms on the complete Theory-27 preparation carrier. Issue 017.20 requires that absence to be reported rather than repaired by inventing a target action.

The rival search also prevents a false uniqueness claim. A coordinate permutation

```text
0 ↔ 1
2 ↔ 3
all other coordinates fixed
```

preserves both imported diagonal effects and maps representative A to B. Thus A and B are projectively distinct but lie in one explicit fixed-test symmetry orbit. That symmetry is not certified as the full OT representation gauge. The rivals reject C3; they do not decide C1 versus C2 after conditional test supply.

## E. Preparation validity

Every conditional output is a nonzero exact rational ray in the 16-dimensional real carrier used by 017.18. The executable witnesses have squared norm exactly `1`.

For all 49 inputs under both A and B:

```text
coordinate type                 Fraction
carrier dimension               16
nonzero ray                     yes
Born values in [0,1]            yes
distribution total              exactly 1
implicit normalization/repair   none
```

This verifies conditional preparation and evaluation validity. It does not convert the constitutive representative choice into a source-authorized preparation theorem.

## F. Fixed-test pipeline

The imported conditional test is one immutable complementary pair:

```text
incident effect      diag(1,1,0,...,0)    rank 2
nonincident effect   diag(0,0,1,...,1)    rank 14
sum                   I_16 exactly
```

Neither effect varies with the input. Every incident input evaluates to `(1,0)` and every nonincident input to `(0,1)`, exactly.

The resulting pipeline is mathematically type-correct **after** constitutive test selection and slot binding:

```text
Point × Line
  → conditional preparation representative
  → imported fixed test labeled as IncidenceDecision
  → exact distribution.
```

It is not source-authorized. There are two label-to-slot bijections before semantic binding, and current authority selects neither the projector test nor that binding for `IncidenceDecision`. Therefore this phase is evaluator conformance only, not a positive Type-Schema-to-OT pipeline.

## G. Canonicality

The task-level verdict is:

```text
C∅ — unavailable under the evidence fence
```

Reason: no source-authorized map selects either a Theory-27 preparation or an `IncidenceDecision` formal test. The executable family begins only after those missing constitutive choices are supplied.

The rejected alternatives are:

- **C0:** choosing concrete basis rays directly is an arbitrary embedding witness, not a scientific positive.
- **C1/C2 conditional ambiguity:** source factorization constrains the conditional family, but the exhibited rivals share a fixed-test symmetry orbit and that stabilizer is not certified as full OT gauge.
- **C3:** impossible to claim because no unique representative is selected and explicit distinct witnesses exist.

This is scoped C∅, not a global impossibility theorem. A later authority could supply either missing bridge.

## H. Residue

The exact ledger is:

| Layer | Content |
|---|---|
| Declared | two seven-element sorts; 21 incidences; 49 inputs; incidence-preserving presentation equivalence; binary incidence semantics |
| Derived | Fano laws; automorphism order 168; 21/28 relation classes; conditional factorization; failure when relation meaning is erased |
| Still constitutive | formal-test selection; one of two slot bindings; preparation representatives; target action/equivalence for source automorphisms |

The **full task residue is not typed sharply enough to quantify** because current authority does not define the admissible source-authorized `IncidenceDecision` test family or its relation to preparation gauge.

A useful conditional count is retained with its conditions visible. After fixing the imported rank-(2,14) test and its slot binding:

```text
ambient real family                       RP^1 × RP^13
ambient real dimension                    14
ambient cardinality                       continuum
executable exact-rational subset          P(Q^2) × P(Q^14)
exact-rational cardinality                countably infinite
per-instance fixed-test-compatible dim    21·1 + 28·13 = 385
schema-invariant conditional dim          1 + 13 = 14
conditional reduction                     371 dimensions
```

The 371-dimensional reduction is real but conditional. It must not be reported as the total source-to-OT residue or as a training-economy result.

## I. Control

The relation-erased control preserves:

```text
all 7 Point identifiers
all 7 Line identifiers
all 49 admitted raw pairs
all 21 incidence rows as byte-identical opaque payload
```

It removes only the declaration that those 21 rows mean point-line incidence. The **same `compile_datum` entry point** is then invoked. It fails closed:

```text
IncidenceDecision is undefined without a declared incident relation
```

The structured automorphism order is 168. With only the two typed seven-element carriers, it becomes

```text
7! × 7! = 25,401,600
```

and the independent sort action is transitive on all 49 point-line pairs. The exact expansion factor is 151,200. Thus incidence meaning is load-bearing for the two-class source quotient; retaining its raw bytes without type semantics does not license the construction.

## J. Training handoff

**No Training handoff is justified.**

The binary decision is already symbolically determined by the declared incidence rule. The only quantified preparation residue, `RP^1 × RP^13`, appears after importing an unauthorized test and is invisible to that sole test. Current authority therefore supplies no typed loss or observation that should select a point in it. Training that ambiguity away would violate the owner/canary fence.

A successor task becomes justified only after an independently authorized principle supplies at least one of:

1. a natural source-to-preparation map or restricted preparation family;
2. a certified target action/equivalence corresponding to source automorphisms; or
3. a richer independently declared formal-test family that operationally distinguishes the residual preparation choices.

Until then, the fixture-scale binary decision is fully symbolic while the OT preparation constitution remains unavailable.

## Machine artifacts

| Artifact | SHA-256 |
|---|---|
| `source_fixture.json` | `20176b9043516e704fbffec84d01f515f008630aef9398d0f44d3cf96886d71c` |
| `audit_input_constitution.py` | `4b3508d04603a426750cdfeff77d6416d8b6b2210e5f499fe3831889b8710d29` |
| `test_audit_input_constitution.py` | `41eded429db406710e74cf6273963658efbd960157c528d7d2dd487143f99a22` |
| `input_constitution_audit.json` | `48c95b16fd3d8f68fa20afcb67df5b54b9512e33380ffc610221d56446c0cf14` |
| attachment `README.md` | `ca26537023f4c30dfd8a2d730f44bcfbef9791b894d55d87ab5aa3318a698dcf` |

Audit schema: `gpt-01720-input-constitution-audit/v2`.

## Validation record

| Validation | Result |
|---|---|
| canonical audit replay | passed; artifact SHA-256 exact |
| focused Issue-017.20 tests | 6 passed |
| targeted Ruff `E4,E7,E9,F,I` | passed after one unused-import correction in the initial C1 draft |
| root `uv lock --check` | passed; 14 packages resolved |
| Decision Model `uv lock --check` | passed; 12 packages resolved |
| full Decision Model suite | 82 passed |
| `git diff --check` | passed |

Execution used Python 3.12.11 on macOS. No network was needed for the reproducibility run. No optimizer, learned parameter, predictive sweep, Interact code, or stable API change was introduced.

## Reproduction

From the repository root:

```bash
PYTHONPATH=decision-model/src uv run --frozen python issues/017-generalized-born-rule/017.20-Code-attachments/audit_input_constitution.py --check
PYTHONPATH=decision-model/src uv run --frozen pytest issues/017-generalized-born-rule/017.20-Code-attachments/test_audit_input_constitution.py
uv run --frozen ruff check --select E4,E7,E9,F,I issues/017-generalized-born-rule/017.20-Code-attachments
```

## Evidence fence

- `occurrence/gpt@e9ccdfdb1b4cdc6e342197d13b68c4d4c493997b495f4e83c5cceb553943bb60`: top-level 15; 017.03; 017.04; 017.18; 017.19; 017.20; 017.20a; 017.20b; banked Issue 011.
- `occurrence/theory@b14aeb1bb9e07662e0e6fdde697f728cae3457324c0e2abd0cfd42975b8aac9a`: Theory 27; Theory 41; additionally named Theory 43 representation boundary.
- Repository base `853f0371f675d911a2988f63c3eaeb5d2901bf6e`: exact 017.18 evaluator witness and stable Decision Model contract.

The terminal statement is:

```text
source fixture gate passes;
source Fano structure is exact and load-bearing;
a zero-training fixed-test plumbing witness can be constructed conditionally;
that witness imports the missing test and representative choices;
its rivals do not settle the certified target quotient;
no source-authorized preparation compiler is available;
C∅ is the strongest task-level verdict;
no Training handoff is earned.
```
