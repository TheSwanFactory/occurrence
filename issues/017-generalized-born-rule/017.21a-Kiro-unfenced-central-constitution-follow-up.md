# 017.21a — Kiro result: dropping the evidence fence, the central-constitution follow-up

- **Executed:** 2026-09-22
- **Executor:** Kiro
- **In-Reply-To:** `017.20-Task-rigorous-input-Type-Schema-to-OT-preparation-compiler-audit.md`
- **Supersedes nothing:** `017.21` stands as the fenced result
- **Prior result commit:** `occurrence@38cd808fe459f7a1e39d9a2ce4f15f55ebf22c5d`
- **Immutable implementation commit:** `occurrence@4906404dbf3621c88b39eb63d9c763606b61e658`
- **Kind:** unfenced formal follow-up; zero learned parameters; no Interact; no physical claim
- **Disposition:** **CONDITIONAL PARTIAL POSITIVE — C1 AT THE REDUCED STATE LAYER, ONE ORIENTATION BIT, AND NO CANONICAL AMBIENT RAY**

## 0. What "dropping the fence" was allowed to mean

`017.21` returned C∅ because the closed evidence list supplied no bridge from the frozen Fano schema to an OT preparation. This turn drops **only** that closed-list restriction and permits deriving new target mathematics.

It does **not** drop the scientific discipline. Retained unchanged: the byte-frozen source, zero training, no per-instance target leakage, naturality/canonicality, one input-independent test, adversarial rival search, residue accounting, the matched relation-erasure control, and the ban on physical or predictive claims.

Three premises are therefore **new declarations, not certified authority**, and are frozen in `declared_premises.json` before any construction:

| ID | Premise | Status |
|---|---|---|
| P1 | `E` is the certified reduced unital ordered Jordan algebra, `Φ(E) = Sym₂(ℝ) ⊕ ℝ` | certified upstream (`occurrence/fixed` 06) |
| P2 | admissible formal effects are the **whole** interval `[0, 1_E]` | **declared here** |
| P3 | the compiler codomain is `S(E)`, not a raw ray of `P(ℝ¹⁶)` | **declared here** |
| P4 | a compiled preparation is the **unique target-automorphism-invariant** extension of the selected central character | **declared here, load-bearing** |

One quantity is deliberately left unresolved and is enumerated rather than defaulted: the **central orientation** `R1`.

## 1. The bridge

The center of `E` is not chosen; it is derived. A central element must act as a scalar on the simple `Sym₂` ideal, so it has the form `(α I₂, β)`; Jordan idempotence forces `α, β ∈ {0,1}`. That gives exactly four central idempotents and therefore exactly **two** minimal nonzero proper ones:

```text
z_M = (I₂, 0)     ideal dim 3    ambient rank 4
z_s = (0,  1)     ideal dim 1    ambient rank 12
z_M ∘ z_s = 0     z_M + z_s = 1_E   both in [0, 1_E]
```

This is an **intrinsic, unordered, input-independent binary formal test** — derived from the target algebra, not imported from `017.18`.

Each minimal central idempotent carries a character of the center. Under P4 each character has exactly one invariant extension:

```text
τ_M(A, s) = Tr(A)/2        τ_M(z_M) = 1   τ_M(z_s) = 0
δ_s(A, s) = s              δ_s(z_M) = 0   δ_s(z_s) = 1
```

Both are exact positive unital functionals. The compiler is then forced once an orientation `β` is fixed:

```text
C_β(point, line) = τ_M or δ_s, selected by the source relation class
```

Every one of the 49 admitted inputs evaluates exactly, with totals exactly `1`, under one immutable test. Zero learned parameters; no per-instance table.

## 2. Exact ambient realization, and its limit

Both states are realized exactly in the 16-dimensional Fixed frame using exact `Q(√7)` arithmetic — not floats, and not merely rationals:

```text
τ_M  ←  u0 + v8 = e0 − (√7/7) · Σ_{i=9..15} e_i     squared norm exactly 2
δ_s  ←  any nonzero ray of W⁻, e.g. e1 − e2
```

A **same-copy control** confirms the cross-copy convention is load-bearing: `u0 + v0` has a nonzero `g1` coordinate and is *not* the invariant state.

But realization is not selection. Sweeping `W⁻` exhibits **21 distinct rays that all induce `δ_s`**; the full fibers are a `PO(2)` family for `τ_M` and `P(W⁻) = ℝP¹¹` for `δ_s`. So:

```text
no canonical ambient preparation ray exists
```

Upstream calls the stabilizer `K_pt = O(2) × O(12)` a gauge *candidate* relative to the observables, not certified gauge. The `017.21` raw-ray layer is therefore **not repaired**.

## 3. Why P4 is necessary, not decorative

This is the preregistered falsifier that mattered most. Consider

```text
ω_t(A, s) = t·A₁₁ + (1−t)·A₂₂
```

Sweeping `t = 0, 1/12, …, 1`: **13 of 13** members are valid states that reproduce the exact central distribution `(1, 0)`, yet only **`t = 1/2`** is target-automorphism invariant.

Without P4 the binding-conditioned compiler is a **continuum**, not unique. Unconditional C3 is therefore false, and P4 is an independently required premise rather than a restatement.

## 4. Equivariance, scoped honestly

`GL(3,2) ≅ PSL(2,7)` is nonabelian simple, hence perfect. `Aut(E, E₊, 1_E) = O(2)/{±I}` is solvable. A perfect group has trivial image in a solvable group, so **every** homomorphism is trivial. Equivariance thus reduces to constancy on the two source orbits, which the compiler satisfies.

The honest scope limit: this says no richer Fano structure is **observable in `E` or `S(E)`**. It does **not** say the ambient carrier lacks Fano-equivariant structure. A counterwitness exists — 168 ambient coordinate permutations acting on `e1..e7` by the point action and `e9..e15` by the induced line action fix all four Fixed generators, so they are faithful yet invisible to `E`. That is not a compiler, because identifying opaque source labels with those coordinates is itself constitutive.

## 5. Control

The relation-erased control preserves both typed carriers, all 49 admitted inputs, and all 21 rows as byte-identical opaque payload, removing only their incidence *meaning*. The **same** `compile_datum` entry point then fails closed, **before any binding is chosen**:

```text
IncidenceDecision is undefined without a declared incident relation
```

Declared symmetry expands `168 → 7!·7! = 25,401,600` (factor 151,200) and all 49 inputs collapse to one orbit. The relation remains load-bearing; the canonical target structure does not rescue an untyped source.

## 6. Verdict by layer

| Layer | Grade | Conditions |
|---|---|---|
| unordered central test | **C3** | P1, P2 |
| state after a binding | **C3** | P1–P4 |
| joint labeled compiler + test | **C1** | P1–P4, one orientation bit |
| raw ambient ray compiler | **C0-or-C∅** | continuous fibers remain |
| physical realization | **C∅** | unchanged from `017.21` |

Rejected: **C2**, because target automorphisms cannot swap nonisomorphic ideals of dimension 3 and 1 (verified: `z_M` is *fixed*, not merely unswapped, by every sampled automorphism); and **unconditional C3**, because the orientation is never selected.

## 7. Residue

```text
declared        sorts, 21 incidences, 49 inputs, presentation equivalence, binary semantics
derived         Fano laws; order 168; orbits 21/28; the center; the canonical test;
                the unique invariant extensions; exact distributions; trivial target action
constitutive    the orientation bit; premises P2/P3/P4; any ambient ray; physical realization
```

The measurable improvement over `017.21`:

```text
017.21   conditional residue   RP¹ × RP¹³    real dimension 14    continuum
017.21a  state-level residue   2 elements    1 specification bit  discrete
```

A 14-real-dimensional conditional family collapses to a two-element discrete choice — **at the `S(E)` layer only**. The raw-ray layer is unimproved.

## 8. Training handoff

**Still not justified.** Both bindings produce **identical** decision behavior on all 49 inputs, verified exhaustively. The surviving residue is one discrete orientation bit that is invisible to the declared decision surface, so no typed training signal can prefer one binding. What changed is that the residue is now finite and named rather than a continuum — it is still not a learning target.

## 9. Is this an improvement over C∅?

Yes, but narrowly and conditionally. The gain is that a **canonical test and a binding-unique preparation now exist**, derived from the target algebra rather than imported. The cost is three explicit new premises, one of which (P4) is provably indispensable. If a reviewer rejects P2, P3, or P4, the result reverts to `017.21`'s C∅ without contradiction.

The honest headline:

> After declaring the full formal effect model, the `S(E)` codomain, and invariant central-character extension, Fano incidence admits exactly two inequivalent central bindings — one orientation bit. Each binding yields a unique abstract state compiler and identical canonical decision behavior. No canonical ambient ray, no physical bridge, and no richer equivariant Fano observable follows.

## Machine artifacts

| Artifact | SHA-256 |
|---|---|
| `declared_premises.json` | `3cb2b3f4decba679dffa05ebe72ded9a7b9645b7df8bb4c1e2bd87cd4198e921` |
| `audit_central_constitution.py` | see attachment README / recomputable |
| `test_audit_central_constitution.py` | see attachment README / recomputable |
| `central_constitution_audit.json` | `e9581c25db9f82dbbba6bb1fe800e3d3b94c92d20fc394ab8adb48ec0d1ff59c` |

Audit schema: `gpt-01721a-central-constitution-audit/v1`. All 36 mechanical checks pass.

## Validation record

| Validation | Result |
|---|---|
| canonical audit replay (`--check`) | passed, byte-exact |
| focused `017.21a` suite | 13 passed |
| preregistered falsifier sweep (F1–F5) | none triggered |
| `017.20` regression suite | 6 passed |
| Decision Model suite | 82 passed |
| `topographo` Fixed-head suite | 4 passed |
| Ruff `E4,E7,E9,F,I` | passed |
| `uv lock --check` | passed, 14 packages |
| `git diff --check` | passed |

Python 3.12.11 on macOS. No network, optimizer, learned parameter, NumPy, or Interact import in the audit path; arithmetic is `Fraction` and exact `Q(√7)`.

## Reproduction

```bash
uv run --frozen python issues/017-generalized-born-rule/017.21a-Code-attachments/audit_central_constitution.py --check
uv run --frozen pytest issues/017-generalized-born-rule/017.21a-Code-attachments/test_audit_central_constitution.py
uv run --frozen ruff check --select E4,E7,E9,F,I issues/017-generalized-born-rule/017.21a-Code-attachments
```

## Evidence record

- `occurrence/fixed@f11ea77410fc79c8bacd8ba44633270786735ecf08cfff05e855613936b1760b`, surface `06-reduced-ordered-space-and-liftable-dynamics.md`, SHA-256 `17e07af79a56cea92343a629bf901ef8c930e5a7bfe0572396968fb308a100ce` — the Jordan isomorphism, `Aut(E,E₊,u) = O(2)/{±I}`, `K_pt = O(2) × O(12)`, multiplicities 2 and 12.
- `occurrence/theory@b14aeb1bb9e07662e0e6fdde697f728cae3457324c0e2abd0cfd42975b8aac9a` — Theory 27 `P(V) → S(E)` factorization and formal test typing; Theory 41 formal-versus-physical boundary.
- `occurrence/gpt@65dca265e5a6d9f0f5937f014a286d361721cd64d37f140ee87d7f2caaf41cbd` — the `017.21` C∅ baseline and the frozen source.

## Claim fences

1. No physical Test Realization or Outcome constitution.
2. No predictive or accuracy claim; the schema rule already fixes every answer.
3. No training-economy claim.
4. No assertion that the ambient carrier lacks Fano-equivariant structure.
5. The result depends on declared premises P2, P3, P4 and **fails without P4**.
6. The `S(E)` gain does not repair the raw-ray or physical layers.

```text
the center is derived, not chosen;
the canonical binary test is intrinsic to E;
each binding yields a unique invariant state compiler;
exactly one orientation bit survives, and it is unobservable;
no canonical ambient ray and no physical bridge are earned;
C∅ still stands at the raw-ray and physical layers.
```
