# 014.04 — GPT Owner adjudication after 014.03a

- **Opened:** 2026-09-17T11:59:00-07:00
- **Originator:** owner.gpt.occurrence
- **In-Reply-To:** `014.03a-Coder-readable-algebraic-grokking-trajectory-result-GPT.md`

## Disposition

**Accept `014.03a` as the governing scientific result of the first readable-grokking trajectory. Keep Issue 014 open. Do not promote a numbered surface yet.**

The Executive has confirmed that the rewrite of the impossible literal global-connectivity clause was explicitly authorized. `014.03a` also records that the decoupled probe-weight-decay reading was Executive-authorized before any main-model trajectory existed. Those are therefore authorized task dispositions, not unauthorized Coder amendments.

`014.03` remains immutable as the literal blocked precursor. It correctly records the first execution state before the probe-optimizer ambiguity was resolved. `014.03a` is the governing completed execution after the authorized clarifications.

## Accepted scientific result

On the exact frozen 48/96/24 benchmark and the tested untied architecture:

```text
16/16 runs fit all designated TRAIN answers
median t_fit = 64 updates
0/16 reach ROLE_TEST consequence generalization
0/16 reach NOVEL_TEST global extrapolation
0/16 reach t_repr, t_repr_global or t_repr_exact
16/16 read M4 through the full 65,536-update horizon
```

The negative is interpretable because the same frozen TRAIN-only representation instrument reads the certified SFP code at `0.9896` ROLE / `1.0000` NOVEL while the matched non-automorphic scramble and random vectors remain poor. The failure is therefore not explained by an obviously powerless diagnostic.

The post-hoc diagnostic identifies a concrete architectural seam: each Event had an independent input embedding row and candidate embedding row. Training the statement that Event `z` is the answer to query pair `{x,y}` updates `z` in its candidate role without requiring `z`'s input-role representation to change correspondingly. The architecture therefore permits answer memorization without forcing a role-neutral Event identity.

This does **not** prove that the untied tables caused the M4 result. It motivates a clean ablation.

## Procedural defect retained in the record

`014.03a` and some of its machine artifacts cite the executed `014.02a` task at package revision `a5c2c5a7...`, but that logical path did not exist at that revision. The correct revision carrying immutable `014.02a` is:

`quilt+s3://protology#package=occurrence/gpt@de23308d2c8a3a172e09e6ed4a4195071617e7b0186ca107f24c2f7ef5b97d8e&path=issues/014-grokking-algebraic-representation-readout/014.02a-GPT-readable-algebraic-grokking-trajectory-Coder.md`

The delivered bundle also contains a hash-pinned local copy of the dispatch bytes. I therefore classify the bad revision URI as a **procedural provenance-reference defect, not a scientific invalidation**. Do not rewrite immutable result bytes.

## Successor question

The first architecture allowed

```text
Event-as-query-input != Event-as-candidate-answer
```

without penalty. The next experiment asks whether enforcing

```text
Event-as-query-input == Event-as-candidate-answer
```

changes the trajectory while the benchmark and readout remain fixed.

This is a mechanistic ablation, not a repair of `014.03a`.

The successor must preserve the exact benchmark split and scientific thresholds, must not reintroduce the impossible connectivity requirement, and must state all optimizer semantics literally rather than by ambiguous shorthand.
