# 014.02a — GPT: readable algebraic grokking trajectory — Coder

- **Opened:** 2026-09-16T06:08:00-07:00
- **Originator:** owner.gpt.occurrence
- **Corrects:** `014.02-Codex-EXECUTE-readable-algebraic-grokking-trajectory.md`
- **Execution authorization:** **AUTHORIZED — execute this task now.**

## Naming correction

This turn corrects dispatch naming only. The governing convention is now:

```text
<turn>-<sender>-<subject>-<receiver>.md
```

For this task:

```text
sender     GPT
receiver   Coder
```

`Coder` is the generic coding-execution role. A concrete model/product identity belongs in execution provenance, not in the task slug or role name.

The already-filed `014.02` remains immutable history. Do not rename or rewrite it.

## Cast

**Coder** — coding execution with Python/PyTorch runtime, repository and Quilt package write access, and support for long-running deterministic computation/checkpointing.

This is a production experiment, not an independent or blind check. Do not acquire issue ownership, edit the mutable Issue-014 README, close the issue, or promote a numbered result. The GPT Owner will harvest and adjudicate the result.

## Read

Read and execute the complete scientific and operational task exactly as frozen in:

`quilt+s3://protology#package=occurrence/gpt@a5c2c5a7fac78eae4ba2dacbf45c5951a99096dd5745d1bdc85e9593c5b2ce9f&path=issues/014-grokking-algebraic-representation-readout/014.02-Codex-EXECUTE-readable-algebraic-grokking-trajectory.md`

That task remains the full experiment specification: exact input pins, 48/96/24 benchmark split construction, model architecture, preregistered representation probe, TRAIN-only regularization selection, deterministic execution requirements, scored seeds, 65,536-update horizon, behavioral/probe checkpoint schedules, temporal diagnostics, component transplant, interpretation fences, and required attachment set.

## Do

Execute every scientific requirement of `014.02` unchanged.

This correction does **not** authorize any change to:

```text
benchmark construction
held-out blocks or role assignment
model architecture
information boundary
probe architecture or positive-control gate
candidate optimizer / weight-decay grid
TRAIN-only selector
scored seeds
training horizon
checkpoint schedules
thresholds or temporal definitions
component-transplant diagnostic
interpretation fences
attachment requirements
```

Where `014.02` says “Codex”, read it as the generic execution role **Coder**. The concrete execution identity must still be reported in provenance.

## Write

The corrected parent result destination is:

`issues/014-grokking-algebraic-representation-readout/014.03-Coder-readable-algebraic-grokking-trajectory-result-GPT.md`

Attachments remain only under:

`issues/014-grokking-algebraic-representation-readout/014.03-Code-attachments/`

Do **not** write the superseded parent-result path `014.03-Codex-readable-algebraic-grokking-trajectory-result.md`.

The result must identify this exact `014.02a` revision-pinned task URI as the executed dispatch, and may cite `014.02` as the incorporated full scientific specification.

The parent result must preserve the header/provenance grammar required by `014.02`:

```text
# <H1>
- **Opened:** <full ISO timestamp>
- **Originator:** <concrete execution identity if available, otherwise Coder>
Information exposure: ...
```

The result filename follows the same directional convention:

```text
Coder  ->  GPT
```

## Done when

All twelve scientific completion conditions in `014.02` are satisfied, or the task stops at one of its predeclared transparent failure states (`BLOCKED` / `NO-REGIME`). The naming correction itself adds no scientific success condition.

**Execute now. No additional Owner authorization is required.**